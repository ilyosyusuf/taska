import 'package:flutter/material.dart';

class FullInfoPage extends StatelessWidget {
  const FullInfoPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}